// Dado um array de números, mostre a soma dos némeros impares

const numeros = [1, 2, 3, 4, 5, 7]

/*
let soma = 0;

for(let i = 0; i < numeros.length; i++){

    const numerosSomados = numeros[i]

    if(numerosSomados % 2 === 1){

        soma += numerosSomados
    }
}
*/
console.log(numeros.reverse())
