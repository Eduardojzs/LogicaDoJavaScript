// Arrays multiplicadas

let numeros = [1, 2, 5, 7, 6];
let numeros2 = [];
for(let i = 0; i < numeros.length; i++){

    let = numerosMultiplicado = numeros[i] * 2;

    numeros2.push(numerosMultiplicado);
}

console.log(numeros)
console.log(numeros2);